{
  "name": "@solana-builder-night/control-plane-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretypecheck": "npm install --no-audit --no-fund --ignore-scripts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
